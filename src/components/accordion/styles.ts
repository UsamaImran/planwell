import { IStyles } from '@/types/global';

export const styles = {
  accordionContainer: {
    backgroundColor: 'transparent',
    boxShadow: 0,
    padding: 0,
    margin: 0,
    borderRadius: 4,
  },
  accordionTitle: {
    textAlign: 'center',
    display: 'flex',
    justifyContent: 'center',
    width: '100%',
    color: 'gray',
  },
} satisfies IStyles;
